# Frontend Next.js Skeleton

Đây là thư mục dành cho Next.js frontend.

Khuyến nghị tạo app:

```bash
npx create-next-app@latest apps/frontend-nextjs --ts --eslint --app
```

Sau đó tổ chức theo `docs/10-frontend-nextjs-design.md`.
